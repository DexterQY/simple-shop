server:
  port: 8081
spring:
  profiles:
    active: dev
  application:
    name: simple-auth
  cloud:
    nacos:
      config:
        file-extension: yml
        group: DEFAULT_GROUP
        extension-configs:
          - data-id: common.yml
            group: DEFAULT_GROUP
            refresh: true